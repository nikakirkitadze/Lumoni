import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('Lumoni app smoke test', (WidgetTester tester) async {
    // Basic smoke test - verifies the test framework works.
    expect(1 + 1, equals(2));
  });
}
